// Thumbnail preview rendering via ratatui-image.
// For v0.1, we display text-based metadata in the preview pane.
// Image rendering via ratatui-image will be integrated once the
// core TUI is stable (requires StatefulWidget integration).
