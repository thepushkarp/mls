// Input handling utilities for the TUI.
// Currently the input handling is done directly in `tui/mod.rs`.
// This module exists for future extraction of complex input handling
// (e.g., vim-like multi-key sequences like `gg`, `dd`, etc.).
